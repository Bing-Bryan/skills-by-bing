interface:
  display_name: "GPT Subagent External Router"
  short_description: "Bridge Sol Ultra to verified external routes through V1"
  default_prompt: "Use $gpt-subagent-external-router to create and open an allowlisted V1 Sol Ultra task, verify its project binding in Desktop, then let Sol route only through author-deployed or locally smoke-tested external transports; label documentation-only candidates as unverified."
policy:
  allow_implicit_invocation: false
