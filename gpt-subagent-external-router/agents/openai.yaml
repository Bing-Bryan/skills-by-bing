interface:
  display_name: "GPT Subagent External Router"
  short_description: "Power project-pinned new entries for V1 Sol external routing"
  default_prompt: "Configure this Skill once, then use it only behind a project-bound pinned Codex entry: reply ENTRY_READY on startup, accept only exact new, launch one allowlisted V1 Sol Ultra task, verify its Desktop project binding, and let Sol route only through author-deployed or locally smoke-tested external transports. Do not expose the launcher as a user command."
policy:
  allow_implicit_invocation: true
